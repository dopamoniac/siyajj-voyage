{
  "buildCommand": "pnpm --filter @workspace/siyajj exec -- vite build --config vite.config.ts --outDir dist",
  "outputDirectory": "artifacts/siyajj/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
